# 댓글 기능 추가 - 주요 변경사항 정리

> **강의 진행 시 빠른 파악을 위한 핵심 내용 정리**

---
spring_blog_8_2026
## 📋 목차

1. [댓글 도메인 패키지 생성](#1-댓글-도메인-패키지-생성)
2. [댓글 엔티티 설계 (단방향 관계)](#2-댓글-엔티티-설계-단방향-관계)
3. [ReplyRepository 생성 (JOIN FETCH)](#3-replyrepository-생성-join-fetch)
4. [ReplyService 생성](#4-replyservice-생성)
5. [ReplyController 생성](#5-replycontroller-생성)
6. [ReplyRequest, ReplyResponse DTO 생성](#6-replyrequest-replyresponse-dto-생성)
7. [BoardController 수정 (댓글 목록 조회 추가)](#7-boardcontroller-수정-댓글-목록-조회-추가)
8. [게시글 상세보기 템플릿 수정](#8-게시글-상세보기-템플릿-수정)
9. [WebMvcConfig 수정 (댓글 URL 패턴 추가)](#9-webmvcconfig-수정-댓글-url-패턴-추가)
10. [게시글 삭제 시 외래키 제약조건 해결](#10-게시글-삭제-시-외래키-제약조건-해결)
11. [데이터베이스 예외 처리 추가](#11-데이터베이스-예외-처리-추가)
12. [한글 인코딩 설정](#12-한글-인코딩-설정)
13. [전체 흐름 정리](#13-전체-흐름-정리)

---

## 1. 댓글 도메인 패키지 생성

### 1.1 패키지 구조

**위치**: `src/main/java/org/example/demo_ssr_v1_1/reply/`

```
reply/
├── Reply.java              # 댓글 엔티티
├── ReplyRepository.java    # 댓글 Repository
├── ReplyService.java       # 댓글 Service
├── ReplyController.java    # 댓글 Controller
├── ReplyRequest.java       # 댓글 요청 DTO
└── ReplyResponse.java      # 댓글 응답 DTO
```

### 1.2 도메인 패키지 설계 원칙

- **도메인별 패키지 분리**: board, user, reply 등 기능별로 패키지 분리
- **계층별 파일 구성**: 각 도메인마다 Entity, Repository, Service, Controller, DTO 포함
- **단일 책임 원칙**: 각 클래스는 하나의 책임만 가짐

---

## 2. 댓글 엔티티 설계 (단방향 관계)

### 2.1 Reply 엔티티 구조

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/Reply.java`

```java
@Data
@NoArgsConstructor
@Table(name = "reply_tb")
@Entity
public class Reply {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(length = 500)
    private String comment;  // 댓글 내용 (최대 500자)
    
    // 단방향 관계: Reply -> Board (N:1)
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "board_id")
    private Board board;
    
    // 단방향 관계: Reply -> User (N:1)
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id")
    private User user;
    
    @CreationTimestamp
    private Timestamp createdAt;
}
```

### 2.2 단방향 관계 설계 이유

| 이유 | 설명 |
|------|------|
| **단순성** | 양방향 관계보다 단순하고 명확함 |
| **복잡성 제거** | 불필요한 양방향 참조로 인한 복잡성 제거 |
| **성능 최적화** | 필요한 경우에만 조회 (LAZY 로딩) |
| **유지보수성** | 코드 이해와 수정이 용이함 |

### 2.3 엔티티 비즈니스 로직

```java
/**
 * 댓글 소유자 확인 로직
 */
public boolean isOwner(Long userId) {
    if (this.user == null || userId == null) {
        return false;
    }
    return this.user.getId().equals(userId);
}

/**
 * 댓글 내용 수정
 */
public void update(String newComment) {
    if (newComment == null || newComment.trim().isEmpty()) {
        throw new Exception400("댓글 내용은 필수입니다");
    }
    if (newComment.length() > 500) {
        throw new Exception400("댓글은 500자 이하여야 합니다");
    }
    this.comment = newComment;
}
```

### 2.4 데이터베이스 제약 조건

- **댓글 내용 길이 제한**: `@Column(length = 500)` - 최대 500자
- **외래키 제약**: `board_id`, `user_id` - NOT NULL
- **생성일 자동 설정**: `@CreationTimestamp` - 자동으로 현재 시간 설정

---

## 3. ReplyRepository 생성 (JOIN FETCH)

### 3.1 Repository 인터페이스

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyRepository.java`

```java
@Repository
public interface ReplyRepository extends JpaRepository<Reply, Long> {
    
    /**
     * 게시글 ID로 댓글 목록 조회 (작성자 정보 포함, JOIN FETCH 사용)
     */
    @Query("SELECT r FROM Reply r JOIN FETCH r.user JOIN FETCH r.board WHERE r.board.id = :boardId ORDER BY r.createdAt ASC")
    List<Reply> findByBoardIdWithUser(@Param("boardId") Long boardId);
    
    /**
     * 댓글 ID로 조회 (작성자 정보 포함, JOIN FETCH 사용)
     */
    @Query("SELECT r FROM Reply r JOIN FETCH r.user JOIN FETCH r.board WHERE r.id = :id")
    Optional<Reply> findByIdWithUser(@Param("id") Long id);
    
    /**
     * 게시글 ID로 댓글 삭제
     * 
     * 게시글 삭제 시 외래키 제약조건 위반을 방지하기 위해
     * 게시글에 속한 모든 댓글을 먼저 삭제하는 데 사용
     * 
     * @param boardId 게시글 ID
     */
    void deleteByBoardId(Long boardId);
}
```

### 3.2 JOIN FETCH 사용 이유

**OSIV False 환경 대응**:
- 트랜잭션 종료 시 세션이 종료되어 LAZY 로딩 불가
- JOIN FETCH로 필요한 데이터를 한 번의 쿼리로 모두 조회

**N+1 문제 해결**:
- 댓글 목록 조회 시 각 댓글의 작성자(User)를 개별 쿼리로 가져오는 대신
- 한 번의 쿼리로 모든 댓글과 작성자 정보를 함께 조회

### 3.3 생성되는 SQL

```sql
-- 댓글 목록 조회
SELECT r.*, b.*, u.* 
FROM reply_tb r 
INNER JOIN user_tb u ON r.user_id = u.id 
INNER JOIN board_tb b ON r.board_id = b.id 
WHERE r.board_id = ?
ORDER BY r.created_at ASC
```

---

## 4. ReplyService 생성

### 4.1 Service 클래스 구조

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyService.java`

```java
@Service
@RequiredArgsConstructor
public class ReplyService {

    private final ReplyRepository replyRepository;
    private final BoardRepository boardRepository;

    // 댓글 목록 조회
    // 댓글 작성
    // 댓글 조회
    // 댓글 삭제
}
```

### 4.2 댓글 목록 조회

```java
@Transactional(readOnly = true)
public List<ReplyResponse.ListDTO> 댓글목록조회(Long boardId, Long sessionUserId) {
    // JOIN FETCH로 Reply, User, Board를 한 번의 쿼리로 함께 조회
    List<Reply> replyList = replyRepository.findByBoardIdWithUser(boardId);
    
    // 트랜잭션 내에서 엔티티를 DTO로 변환
    // 람다 표현식 사용: reply -> new ReplyResponse.ListDTO(reply, sessionUserId)
    return replyList.stream()
            .map(reply -> new ReplyResponse.ListDTO(reply, sessionUserId))
            .collect(Collectors.toList());
}
```

**핵심 포인트**:
- `@Transactional(readOnly = true)`: 읽기 전용 트랜잭션 (성능 최적화)
- JOIN FETCH로 N+1 문제 해결
- Stream을 사용하여 엔티티를 DTO로 변환
- `sessionUserId`를 전달하여 댓글 소유자 여부 확인

### 4.3 댓글 작성

```java
@Transactional
public Reply 댓글작성(ReplyRequest.SaveDTO saveDTO, User user) {
    // 유효성 검사
    saveDTO.validate();
    
    // 게시글 조회
    Board board = boardRepository.findById(saveDTO.getBoardId())
            .orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));
    
    // DTO를 엔티티로 변환
    Reply reply = saveDTO.toEntity(board, user);
    
    // JpaRepository의 save() 메서드
    return replyRepository.save(reply);
}
```

**핵심 포인트**:
- 유효성 검사: DTO의 `validate()` 메서드 호출
- 게시글 존재 여부 확인: 없으면 Exception404 발생
- DTO → Entity 변환: `toEntity()` 메서드 사용

### 4.4 댓글 삭제

```java
@Transactional
public void 댓글삭제(Long id, Long userId) {
    // 댓글 조회 (작성자 정보 포함)
    Reply reply = replyRepository.findByIdWithUser(id)
            .orElseThrow(() -> new Exception404("댓글을 찾을 수 없습니다"));

    // 인가 검사: 댓글 소유자인지 확인
    if (!reply.isOwner(userId)) {
        throw new Exception403("댓글 삭제 권한이 없습니다");
    }

    // JpaRepository의 deleteById() 메서드
    replyRepository.deleteById(id);
}
```

**핵심 포인트**:
- 인가 검사: 댓글 소유자만 삭제 가능
- JOIN FETCH로 작성자 정보 함께 조회
- 소유자가 아니면 Exception403 발생

---

## 5. ReplyController 생성

### 5.1 Controller 클래스 구조

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyController.java`

```java
@RequiredArgsConstructor
@Controller
public class ReplyController {

    private final ReplyService replyService;

    // 댓글 작성: POST /reply/save
    // 댓글 삭제: POST /reply/{id}/delete
}
```

### 5.2 댓글 작성

```java
@PostMapping("/reply/save")
public String saveProc(ReplyRequest.SaveDTO saveDTO, HttpSession session) {
    // 1. 인증 검사: LoginInterceptor가 처리
    User sessionUser = (User) session.getAttribute("sessionUser");

    // 2. Service에 비즈니스 로직 위임
    replyService.댓글작성(saveDTO, sessionUser);

    // 3. 댓글 작성 후 게시글 상세보기로 리다이렉트
    return "redirect:/board/" + saveDTO.getBoardId();
}
```

**핵심 포인트**:
- 인증: LoginInterceptor가 처리 (인증 필요)
- 세션에서 사용자 정보 추출
- 리다이렉트: 게시글 상세보기로 이동

### 5.3 댓글 삭제

```java
@PostMapping("/reply/{id}/delete")
public String delete(@PathVariable Long id, HttpSession session) {
    // 1. 인증 검사: LoginInterceptor가 처리
    User sessionUser = (User) session.getAttribute("sessionUser");

    // 2. 댓글 조회 (게시글 ID를 얻기 위해)
    Reply reply = replyService.댓글조회(id);
    Long boardId = reply.getBoard().getId();

    // 3. Service에 비즈니스 로직 위임
    replyService.댓글삭제(id, sessionUser.getId());

    // 4. 댓글 삭제 후 게시글 상세보기로 리다이렉트
    return "redirect:/board/" + boardId;
}
```

**핵심 포인트**:
- 댓글 조회: 게시글 ID를 얻기 위해 먼저 조회
- 인가 검사: Service 레이어에서 처리
- 리다이렉트: 게시글 상세보기로 이동

---

## 6. ReplyRequest, ReplyResponse DTO 생성

### 6.1 ReplyRequest (요청 DTO)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyRequest.java`

```java
public class ReplyRequest {

    @Data
    public static class SaveDTO {
        private String comment;  // 댓글 내용
        private Long boardId;    // 게시글 ID

        public void validate() {
            if (comment == null || comment.trim().isEmpty()) {
                throw new Exception400("댓글 내용을 입력해주세요");
            }
            if (comment.length() > 500) {
                throw new Exception400("댓글은 500자 이하여야 합니다");
            }
            if (boardId == null) {
                throw new Exception400("게시글 ID가 필요합니다");
            }
        }

        public Reply toEntity(Board board, User user) {
            return Reply.builder()
                    .comment(this.comment)
                    .board(board)
                    .user(user)
                    .build();
        }
    }

    @Data
    public static class UpdateDTO {
        private String comment;  // 댓글 내용

        public void validate() {
            if (comment == null || comment.trim().isEmpty()) {
                throw new Exception400("댓글 내용을 입력해주세요");
            }
            if (comment.length() > 500) {
                throw new Exception400("댓글은 500자 이하여야 합니다");
            }
        }
    }
}
```

**핵심 포인트**:
- 유효성 검사: `validate()` 메서드에서 처리
- 길이 제한: 500자 이하 검증
- DTO → Entity 변환: `toEntity()` 메서드

### 6.2 ReplyResponse (응답 DTO)

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyResponse.java`

```java
public class ReplyResponse {

    @Data
    public static class ListDTO {
        private Long id;
        private String comment;      // 댓글 내용
        private Long userId;         // 작성자 ID
        private String username;     // 작성자명 (평탄화)
        private String createdAt;    // 포맷된 생성일
        private boolean isOwner;     // 댓글 소유자 여부

        public ListDTO(Reply reply, Long sessionUserId) {
            this.id = reply.getId();
            this.comment = reply.getComment();
            // JOIN FETCH로 이미 로딩된 user 사용
            if (reply.getUser() != null) {
                this.userId = reply.getUser().getId();
                this.username = reply.getUser().getUsername();
            }
            // 날짜 포맷팅
            if (reply.getCreatedAt() != null) {
                this.createdAt = MyDateUtil.timestampFormat(reply.getCreatedAt());
            }
            // 댓글 소유자 여부 확인
            this.isOwner = reply.isOwner(sessionUserId);
        }
    }
}
```

**핵심 포인트**:
- 평탄화: `user.username` → `username`
- 날짜 포맷팅: `MyDateUtil.timestampFormat()` 사용
- 소유자 여부: 템플릿에서 삭제 버튼 표시 여부 결정

---

## 7. BoardController 수정 (댓글 목록 조회 추가)

### 7.1 변경 내용

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardController.java`

```java
@RequiredArgsConstructor
@Controller
public class BoardController {

    private final BoardService boardService;
    private final ReplyService replyService;  // 추가

    @GetMapping("/board/{id}")
    public String detail(@PathVariable Long id, Model model, HttpSession session) {
        // 게시글 조회
        BoardResponse.DetailDTO board = boardService.게시글상세조회(id);

        // 세션에서 로그인 사용자 정보 조회
        User sessionUser = (User) session.getAttribute("sessionUser");
        
        // 게시글 소유자 여부 확인
        boolean isOwner = false;
        if (sessionUser != null && board.getUserId() != null) {
            isOwner = board.getUserId().equals(sessionUser.getId());
        }

        // 댓글 목록 조회 (추가)
        Long sessionUserId = sessionUser != null ? sessionUser.getId() : null;
        List<ReplyResponse.ListDTO> replyList = replyService.댓글목록조회(id, sessionUserId);

        // View에 데이터 전달
        model.addAttribute("board", board);
        model.addAttribute("isOwner", isOwner);
        model.addAttribute("replyList", replyList);  // 추가
        return "board/detail";
    }
}
```

**핵심 포인트**:
- `ReplyService` 의존성 주입 추가
- 댓글 목록 조회 추가
- 세션 사용자 ID 전달 (댓글 소유자 여부 확인용)

---

## 8. 게시글 상세보기 템플릿 수정

### 8.1 변경 내용

**파일**: `src/main/resources/templates/board/detail.mustache`

```mustache
<!-- 댓글 -->
<div class="card mt-3">
    <!-- 댓글등록 -->
    {{#sessionUser}}
    <div class="card-body">
        <form action="/reply/save" method="post">
            <input type="hidden" name="boardId" value="{{board.id}}">
            <textarea class="form-control" rows="2" name="comment" placeholder="댓글을 입력하세요"></textarea>
            <div class="d-flex justify-content-end">
                <button type="submit" class="btn btn-outline-primary mt-1">댓글등록</button>
            </div>
        </form>
    </div>
    {{/sessionUser}}
    {{^sessionUser}}
    <div class="card-body">
        <p class="text-muted">댓글을 작성하려면 <a href="/login">로그인</a>이 필요합니다.</p>
    </div>
    {{/sessionUser}}

    <!-- 댓글목록 -->
    <div class="card-footer">
        <b>댓글리스트</b>
    </div>
    <div class="list-group">
        {{#replyList}}
        <div class="list-group-item d-flex justify-content-between align-items-center">
            <div class="d-flex flex-column">
                <div class="d-flex align-items-center mb-1">
                    <div class="px-2 py-1 me-2 bg-primary text-white rounded">{{username}}</div>
                    <small class="text-muted">{{createdAt}}</small>
                </div>
                <div>{{comment}}</div>
            </div>
            {{#sessionUser}}
            {{#isOwner}}
            <form action="/reply/{{id}}/delete" method="post">
                <button type="submit" class="btn btn-sm">🗑</button>
            </form>
            {{/isOwner}}
            {{/sessionUser}}
        </div>
        {{/replyList}}
        {{^replyList}}
        <div class="list-group-item text-muted text-center">
            댓글이 없습니다.
        </div>
        {{/replyList}}
    </div>
</div>
```

### 8.2 템플릿 주요 기능

1. **댓글 작성 폼**
   - 로그인 사용자만 표시 (`{{#sessionUser}}`)
   - 비로그인 사용자에게는 로그인 안내 메시지

2. **댓글 목록**
   - 작성자명, 작성일, 댓글 내용 표시
   - 댓글이 없으면 안내 메시지 표시

3. **댓글 삭제 버튼**
   - 로그인 사용자이면서 댓글 소유자만 표시
   - `{{#isOwner}}` 조건으로 제어

---

## 9. WebMvcConfig 수정 (댓글 URL 패턴 추가)

### 9.1 변경 내용

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/config/WebMvcConfig.java`

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    // SessionInterceptor를 모든 요청에 등록
    registry.addInterceptor(sessionInterceptor)
            .addPathPatterns("/**");
    
    // LoginInterceptor를 시스템에 등록
    registry.addInterceptor(loginInterceptor)
            // 인터셉터가 동작할 URL 패턴 지정
            .addPathPatterns("/board/**", "/user/**", "/reply/**")  // /reply/** 추가
            // 인터셉터에서 제외할 URL 패턴 지정
            .excludePathPatterns(
                    // 로그인 관련
                    "/login", "/join", "/logout",
                    // 게시글 조회 관련
                    "/board/list", "/", "/board/{id:\\d+}",
                    // 정적 리소스
                    "/css/**", "/js/**", "/images/**", "/favicon.ico",
                    // H2 콘솔
                    "/h2-console/**"
            );
}
```

**핵심 포인트**:
- `/reply/**` 패턴 추가: 댓글 작성/삭제는 로그인 필요
- 댓글 조회는 게시글 상세보기에서 함께 조회되므로 별도 URL 패턴 불필요

---

## 10. 게시글 삭제 시 외래키 제약조건 해결

### 10.1 문제 상황

게시글에 댓글이 있는 상태에서 게시글을 삭제하려고 하면 외래키 제약조건 위반 오류가 발생합니다.

```
Referential integrity constraint violation: 
"FKjm6c1ap97ufy07pocvy5m64wj: PUBLIC.reply_tb FOREIGN KEY(board_id) 
REFERENCES PUBLIC.board_tb(id)"
```

### 10.2 해결 방법

**게시글 삭제 전에 해당 게시글의 모든 댓글을 먼저 삭제**해야 합니다.

### 10.3 ReplyRepository 수정

**파일**: `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyRepository.java`

```java
/**
 * 게시글 ID로 댓글 삭제
 * 
 * 게시글 삭제 시 외래키 제약조건 위반을 방지하기 위해
 * 게시글에 속한 모든 댓글을 먼저 삭제하는 데 사용
 * 
 * @param boardId 게시글 ID
 */
void deleteByBoardId(Long boardId);
```

**핵심 포인트**:
- Spring Data JPA의 쿼리 메서드 네이밍 규칙 사용
- `deleteByBoardId` → `DELETE FROM reply_tb WHERE board_id = ?` 쿼리 자동 생성

### 10.4 BoardService 수정

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardService.java`

```java
@Service
@RequiredArgsConstructor
public class BoardService {

    private final BoardRepository boardRepository;
    private final ReplyRepository replyRepository;  // 추가

    @Transactional
    public void 게시글삭제(Long id, Long userId) {
        // 게시글 조회
        Board board = boardRepository.findById(id)
                .orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));

        // 인가 검사: 게시글 소유자인지 확인
        if (!board.isOwner(userId)) {
            throw new Exception403("삭제 권한이 없습니다");
        }

        // 게시글에 댓글이 있으면 먼저 댓글 삭제 (외래키 제약조건 해결)
        // 게시글 삭제 전에 해당 게시글의 모든 댓글을 먼저 삭제
        replyRepository.deleteByBoardId(id);
        
        // JpaRepository의 deleteById() 메서드
        boardRepository.deleteById(id);
    }
}
```

**핵심 포인트**:
- `ReplyRepository` 주입 추가
- 게시글 삭제 전에 댓글 먼저 삭제
- 같은 트랜잭션 내에서 처리 (원자성 보장)

### 10.5 삭제 순서

```
1. 게시글 조회 및 인가 검사
2. 댓글 삭제 (replyRepository.deleteByBoardId())
3. 게시글 삭제 (boardRepository.deleteById())
```

**트랜잭션 보장**:
- `@Transactional`로 인해 댓글 삭제와 게시글 삭제가 하나의 트랜잭션으로 처리됨
- 중간에 오류가 발생하면 자동 롤백

---

## 11. 데이터베이스 예외 처리 추가

### 11.1 문제 상황

데이터베이스 제약조건 위반 오류가 발생했을 때 `@ControllerAdvice`에서 처리되지 않아 기본 오류 페이지가 표시되었습니다.

### 11.2 해결 방법

`MyExceptionHandler`에 `DataIntegrityViolationException` 처리 추가

### 11.3 MyExceptionHandler 수정

**파일**: `src/main/java/org/example/demo_ssr_v1_1/_core/errors/MyExceptionHandler.java`

```java
import org.springframework.dao.DataIntegrityViolationException;

@ControllerAdvice
@Slf4j
public class MyExceptionHandler {

    // 데이터베이스 제약조건 위반 오류 처리
    @ExceptionHandler(DataIntegrityViolationException.class)
    public String handleDataIntegrityViolationException(DataIntegrityViolationException e,
                                                         HttpServletRequest request) {
        log.warn("=== 데이터베이스 제약조건 위반 오류 발생  ===");
        log.warn("요청 URL : {}", request.getRequestURL());
        log.warn("에러 메세지 : {}", e.getMessage());
        log.warn("예외 클래스 : {}", e.getClass().getSimpleName());
        
        // 외래키 제약조건 위반인 경우
        String errorMessage = e.getMessage();
        if (errorMessage != null && errorMessage.contains("FOREIGN KEY")) {
            request.setAttribute("msg", "관련된 데이터가 있어 삭제할 수 없습니다. (예: 게시글에 댓글이 있는 경우)");
        } else {
            request.setAttribute("msg", "데이터베이스 제약조건 위반: " + e.getMessage());
        }
        return "err/500";
    }

    // 기타 모든 실행시점 오류 처리
    @ExceptionHandler(RuntimeException.class)
    public String handleRuntimeException(RuntimeException e,
                                         HttpServletRequest request) {
        // ...
    }
}
```

**핵심 포인트**:
- `DataIntegrityViolationException`: 데이터베이스 제약조건 위반 시 발생하는 예외
- 외래키 제약조건 위반인 경우 사용자 친화적인 메시지 표시
- 다른 제약조건 위반인 경우도 처리

### 11.4 예외 처리 순서

```
1. 커스텀 예외 (Exception400, 401, 403, 404, 500)
2. DataIntegrityViolationException (데이터베이스 제약조건 위반)
3. RuntimeException (기타 모든 실행시점 오류)
```

---

## 12. 한글 인코딩 설정

### 12.1 문제 상황

애플리케이션 실행 시 한글이 깨져서 출력되는 문제가 발생했습니다.

### 12.2 해결 방법

SQL 파일 인코딩 설정과 Java 컴파일 인코딩 설정 추가

### 12.3 application-dev.yml 수정

**파일**: `src/main/resources/application-dev.yml`

```yaml
spring:
  # 초기 데이터 로딩 설정
  sql:
    init:
      # 2.5 이상 버전부터 명시를 해야 insert 처리 됨
      mode: always
      # 애플리케이션 시작시 실행할 SQL 파일 위치
      data-locations:
        - classpath:db/data.sql
      # SQL 파일 인코딩 설정 (한글 깨짐 방지)
      encoding: UTF-8
```

**핵심 포인트**:
- `spring.sql.init.encoding: UTF-8` 설정으로 SQL 파일을 UTF-8로 읽도록 설정
- `data.sql` 파일이 UTF-8로 저장되어 있어야 함

### 12.4 build.gradle 수정

**파일**: `build.gradle`

```gradle
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
    // 소스 파일 인코딩 설정 (한글 깨짐 방지)
    sourceCompatibility = '17'
    targetCompatibility = '17'
}

// 컴파일 시 인코딩 설정
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
```

**핵심 포인트**:
- `tasks.withType(JavaCompile)`: 모든 Java 컴파일 작업에 인코딩 설정 적용
- `options.encoding = 'UTF-8'`: 컴파일 시 UTF-8 인코딩 사용

### 12.5 인코딩 설정 체크리스트

- [ ] `application-dev.yml`에 `spring.sql.init.encoding: UTF-8` 설정
- [ ] `build.gradle`에 `tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }` 설정
- [ ] `data.sql` 파일이 UTF-8로 저장되어 있는지 확인
- [ ] IDE에서 프로젝트 인코딩이 UTF-8로 설정되어 있는지 확인

---

## 13. 전체 흐름 정리

### 10.1 댓글 작성 흐름

```
[사용자] 댓글 입력 후 "댓글등록" 버튼 클릭
  ↓
[Controller] POST /reply/save
  ├─ 세션에서 사용자 정보 추출
  └─ ReplyService.댓글작성() 호출
      ↓
[Service] @Transactional
  ├─ 유효성 검사 (validate())
  ├─ 게시글 조회 (BoardRepository.findById())
  ├─ DTO → Entity 변환 (toEntity())
  └─ 댓글 저장 (ReplyRepository.save())
      ↓
[Controller] 게시글 상세보기로 리다이렉트
  ↓
[사용자] 댓글이 추가된 게시글 상세보기 화면 표시
```

### 10.2 댓글 목록 조회 흐름

```
[사용자] 게시글 상세보기 접근
  ↓
[Controller] GET /board/{id}
  ├─ BoardService.게시글상세조회() 호출
  └─ ReplyService.댓글목록조회() 호출
      ↓
[Service] @Transactional(readOnly = true)
  ├─ ReplyRepository.findByBoardIdWithUser()
  │   └─ JOIN FETCH로 Reply + User + Board 한 번에 조회
  └─ Stream으로 Reply → ListDTO 변환
      └─ DTO 생성자에서 user.username, isOwner 등 추출
      ↓
[Controller] Model에 board, replyList 추가
  ↓
[Template] 댓글 목록 표시
```

### 10.3 댓글 삭제 흐름

```
[사용자] 댓글 삭제 버튼 클릭
  ↓
[Controller] POST /reply/{id}/delete
  ├─ 세션에서 사용자 정보 추출
  ├─ ReplyService.댓글조회() 호출 (게시글 ID 얻기)
  └─ ReplyService.댓글삭제() 호출
      ↓
[Service] @Transactional
  ├─ 댓글 조회 (ReplyRepository.findByIdWithUser())
  ├─ 인가 검사 (reply.isOwner())
  └─ 댓글 삭제 (ReplyRepository.deleteById())
      ↓
[Controller] 게시글 상세보기로 리다이렉트
  ↓
[사용자] 댓글이 삭제된 게시글 상세보기 화면 표시
```

### 13.4 핵심 포인트 정리

| 항목 | 설명 |
|------|------|
| **단방향 관계** | Reply → Board, Reply → User (ManyToOne) |
| **JOIN FETCH** | N+1 문제 해결 및 OSIV False 환경 대응 |
| **DTO 패턴** | 엔티티를 직접 반환하지 않고 DTO로 변환 |
| **인증/인가** | 댓글 작성/삭제는 로그인 필요, 삭제는 소유자만 가능 |
| **유효성 검사** | 댓글 내용 500자 제한 |
| **트랜잭션 관리** | 읽기 전용 트랜잭션 사용 (성능 최적화) |
| **외래키 제약조건** | 게시글 삭제 전 댓글 먼저 삭제 |
| **예외 처리** | DataIntegrityViolationException 처리 추가 |
| **인코딩 설정** | SQL 파일 및 Java 컴파일 인코딩 UTF-8 설정 |

---

## 14. 코드 변경 요약

| 파일 | 변경 내용 |
|------|----------|
| `Reply.java` | 댓글 엔티티 생성 (단방향 관계, 500자 제한) |
| `ReplyRepository.java` | JOIN FETCH 메서드 추가, `deleteByBoardId()` 메서드 추가 |
| `ReplyService.java` | 댓글 목록 조회, 작성, 삭제 기능 |
| `ReplyController.java` | 댓글 작성/삭제 요청 처리 |
| `ReplyRequest.java` | SaveDTO, UpdateDTO 생성 |
| `ReplyResponse.java` | ListDTO 생성 (평탄화, 소유자 여부 포함) |
| `BoardController.java` | 댓글 목록 조회 추가 |
| `BoardService.java` | `ReplyRepository` 주입 추가, 게시글 삭제 시 댓글 먼저 삭제 |
| `detail.mustache` | 댓글 작성 폼, 댓글 목록 표시 |
| `WebMvcConfig.java` | `/reply/**` URL 패턴 추가 |
| `BoardResponse.java` | UpdateFormDTO에 username 필드 추가 |
| `MyExceptionHandler.java` | `DataIntegrityViolationException` 처리 추가 |
| `application-dev.yml` | SQL 파일 인코딩 설정 추가 (`encoding: UTF-8`) |
| `build.gradle` | Java 컴파일 인코딩 설정 추가 (`options.encoding = 'UTF-8'`) |

---

## 15. 학습 체크리스트

### 15.1 개념 이해

- [ ] 단방향 관계와 양방향 관계의 차이
- [ ] JOIN FETCH의 동작 원리
- [ ] N+1 문제와 해결 방법
- [ ] DTO 패턴의 필요성
- [ ] 인증과 인가의 차이

### 15.2 실습

- [ ] Reply 엔티티 생성
- [ ] ReplyRepository에 JOIN FETCH 메서드 작성
- [ ] ReplyService에서 DTO 반환
- [ ] ReplyController에서 요청 처리
- [ ] 템플릿에서 댓글 기능 구현

### 15.3 주의사항

- [ ] JOIN FETCH 사용 시 명시적 LAZY 로딩 트리거 불필요
- [ ] DTO 생성자에서 null 체크 유지
- [ ] 트랜잭션 내에서 DTO 변환 완료
- [ ] 댓글 소유자 여부 확인 로직 구현
- [ ] 유효성 검사 (500자 제한) 구현

---

### 15.4 추가 학습 항목

- [ ] 외래키 제약조건과 CASCADE 옵션의 차이
- [ ] 데이터베이스 예외 처리 방법
- [ ] 인코딩 설정의 중요성과 설정 방법

---

## 16. 참고 자료

### 16.1 관련 파일

- `src/main/java/org/example/demo_ssr_v1_1/reply/Reply.java` - 댓글 엔티티
- `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyRepository.java` - JOIN FETCH 메서드
- `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyService.java` - Service 로직
- `src/main/java/org/example/demo_ssr_v1_1/reply/ReplyController.java` - Controller
- `src/main/resources/templates/board/detail.mustache` - 댓글 UI

### 16.2 핵심 키워드

- **단방향 관계 (Unidirectional Relationship)**
- **JOIN FETCH**
- **N+1 Problem**
- **DTO (Data Transfer Object)**
- **인증 (Authentication) / 인가 (Authorization)**
- **트랜잭션 (Transaction)**

---

**작성일**: 2026년  
**프로젝트**: Spring Blog (SSR v1.1)  
**학습 목표**: 댓글 기능 구현을 통한 단방향 관계, JOIN FETCH, DTO 패턴, 외래키 제약조건, 예외 처리, 인코딩 설정 학습

---

## 17. 최신 변경사항 (2026년 12월)

### 17.1 게시글 삭제 시 외래키 제약조건 해결
- `ReplyRepository`에 `deleteByBoardId()` 메서드 추가
- `BoardService`에서 게시글 삭제 전 댓글 먼저 삭제하도록 수정

### 17.2 데이터베이스 예외 처리 추가
- `MyExceptionHandler`에 `DataIntegrityViolationException` 처리 추가
- 외래키 제약조건 위반 시 사용자 친화적인 에러 메시지 표시

### 17.3 한글 인코딩 설정
- `application-dev.yml`에 SQL 파일 인코딩 설정 추가
- `build.gradle`에 Java 컴파일 인코딩 설정 추가
